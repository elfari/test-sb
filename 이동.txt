// 배열 안의 객체를 REQ_NO로 매칭해서 특정 필드만 업데이트
arrData.forEach(function(item) {
  var valid = validData.find(function(v) {
    return v.REQ_NO === item.REQ_NO;
  });

  if (valid) {
    item.SIM_RSN_CD = valid.SIM_RSN_CD;
    item.SIM_RSN_DTL = valid.SIM_RSN_DTL;
  }
});