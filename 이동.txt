<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS 클래스 아이콘 프로세스</title>
    <style>
        body {
            font-family: 'Pretendard', sans-serif;
            background-color: #f8f9fa;
            padding: 50px 20px;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 60px;
        }

        .process-wrapper {
            width: 100%;
            max-width: 650px;
            background: #fff;
            padding: 60px 30px 80px 30px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .process-list {
            display: flex;
            align-items: flex-start; /* 아이콘 상단 기준 정렬 */
            list-style: none;
            padding: 0;
            margin: 0;
            width: 100%;
        }

        /* 각 단계 세트 */
        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            flex-shrink: 0;
            width: 70px; /* 텍스트 너비 고려 */
        }

        /* 아이콘 공통 클래스 (CSS로 이미지 처리) */
        .ico-img {
            width: 50px;
            height: 50px;
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
            background-color: #fff;
            z-index: 2;
        }

        /* 각 단계별 이미지 클래스 분리 */
        .ico-step1 { background-image: url('https://cdn-icons-png.flaticon.com/512/190/190411.png'); }
        .ico-step2 { background-image: url('https://cdn-icons-png.flaticon.com/512/1149/1149168.png'); }
        .ico-step3 { background-image: url('https://cdn-icons-png.flaticon.com/512/483/483361.png'); }
        .ico-step4 { background-image: url('https://cdn-icons-png.flaticon.com/512/709/709790.png'); }

        /* 상태 텍스트 */
        .txt {
            margin-top: 12px;
            font-size: 14px;
            color: #333;
            font-weight: 600;
            text-align: center;
            white-space: nowrap;
        }

        /* 연결 라인 (중앙 정렬 공식 적용) */
        .line {
            flex: 1;
            height: 2px;
            background-color: #3498db;
            /* 아이콘 높이(50px)의 절반인 25px 위치에 선 배치 */
            margin-top: 24px; 
            margin-left: -10px;
            margin-right: -10px;
            z-index: 1;
        }

        /* 비활성화 상태 (off 클래스 하나로 통합 제어) */
        .off .ico-img {
            filter: grayscale(1);
            opacity: 0.5;
        }
        .off .txt {
            color: #aaa;
            font-weight: 400;
        }
        .line.off {
            background-color: #e0e0e0;
        }

        @media (max-width: 480px) {
            .ico-img { width: 40px; height: 40px; }
            .line { margin-top: 19px; }
            .txt { font-size: 12px; }
            .step { width: 55px; }
        }
    </style>
</head>
<body>

    <div class="process-wrapper">
        <ul class="process-list">
            <!-- 1단계 -->
            <li class="step">
                <div class="ico-img ico-step1"></div>
                <span class="txt">접수완료</span>
            </li>
            
            <li class="line"></li>

            <!-- 2단계 -->
            <li class="step">
                <div class="ico-img ico-step2"></div>
                <span class="txt">검토중</span>
            </li>

            <li class="line off"></li>

            <!-- 3단계 (off 적용) -->
            <li class="step off">
                <div class="ico-img ico-step3"></div>
                <span class="txt">발송대기</span>
            </li>

            <li class="line off"></li>

            <!-- 4단계 (off 적용) -->
            <li class="step off">
                <div class="ico-img ico-step4"></div>
                <span class="txt">배송완료</span>
            </li>
        </ul>
    </div>

</body>
</html>

